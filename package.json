{
  "name": "shot.nearby",
  "version": "0.0.1",
  "description": "Server for shot.nearby client",
  "main": "server.js",
  "scripts": {
    "start": "babel-node server/server",
    "test": "echo \"Error: no test specified\" && exit 1",
    "getCoords": "babel-node server/getCoords --presets es2015,env",
    "heroku-postbuild":
      "cd client && npm install --only=dev && npm install && npm run build"
  },
  "keywords": ["Google", "Maps", "API", "React", "Redux", "React", "Router"],
  "author": "Javed",
  "license": "MIT",
  "dependencies": {
    "express": "^4.16.3",
    "morgan": "^1.9.0",
    "react-images": "^0.5.17"
  },
  "devDependencies": {
    "axios": "^0.18.0",
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babel-preset-es2015": "^6.24.1",
    "uuid": "^3.2.1"
  }
}
